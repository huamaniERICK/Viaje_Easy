import 'package:flutter/material.dart';

class StatelesWidget extends StatelessWidget {
  const StatelesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
